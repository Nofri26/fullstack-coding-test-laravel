<x-app-layout>    <div class="h-screen w-full p-4">        <div class="flex items-center justify-end gap-4">            <div class="col-span-full">                <form action="{{route('posts.search')}}" method="POST">                    @csrf                    <div class="flex items-center rounded-md bg-white pl-3 outline outline-1 -outline-offset-1 outline-gray-300 focus-within:outline focus-within:outline-2 focus-within:-outline-offset-2 focus-within:outline-indigo-600">                        <input type="text" name="search" id="search" class="block min-w-0 grow py-1.5 pl-1 pr-3 text-base text-gray-900 placeholder:text-gray-400 focus:outline focus:outline-0 sm:text-sm/6" placeholder="Laravel News">                        <button type="submit" class="block min-w-0 grow py-1.5 pl-1 pr-3 text-base text-gray-900 bg-slate-100">Search</button>                    </div>                </form>            </div>            <a href="{{route('posts.create')}}" class="bg-black text-white rounded-md hover:bg-gray-600 px-4 py-1">Create</a>        </div>        <div class="grid grid-cols-4 gap-4 w-full justify-center items-center p-4">            @foreach($posts as $post)                <x-cards.card>                    <x-cards.card-header>                        {{$post->title}}                    </x-cards.card-header>                    <x-cards.card-content>                        {{$post->content}}                    </x-cards.card-content>                    <x-cards.card-footer>                        <div class="flex justify-between">                            <div>                                Created by {{$post->author}}                            </div>                            <div class="flex gap-1">                                <a href="{{route('posts.show', $post)}}" class="bg-blue-600 text-white rounded-md hover:bg-blue-800 px-2 py-1">Show</a>                                <a href="{{route('posts.edit', $post)}}" class="bg-yellow-500 text-black rounded-md hover:bg-yellow-800 px-2 py-1">Edit</a>                                <form action="{{route('posts.destroy', $post)}}" method="POST">                                    @method('DELETE')                                    @csrf                                    <button type="submit" class="bg-red-600 text-white rounded-md hover:bg-red-800 px-2 py-1">Delete</button>                                </form>                            </div>                        </div>                    </x-cards.card-footer>                </x-cards.card>            @endforeach        </div>        {{ $posts->links() }}    </div></x-app-layout>